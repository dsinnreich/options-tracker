[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build:all && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
watchPatterns = ["server/**", "client/**", "package.json", "railway.toml"]
